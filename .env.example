# Copy to .env and edit as needed
# Use PostgreSQL for production, e.g. postgres://user:pass@db:5432/salesdb
# For local quick dev the app defaults to sqlite if DATABASE_URL is unset
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/salesdb
PORT=5000
